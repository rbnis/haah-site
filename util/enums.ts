export enum lightState {
  inherit,
  lightOn,
  lightOff,
}

export enum occupancyState {
  unoccupied,
  occupied,
}

export enum windowState {
  closed,
  open,
}

export enum climateMode {
  off = 'off',
  auto = 'auto',
  heat = 'heat'
}
